# Local environment (may contain API keys)
.env

# NeMo Guardrails config is generated at startup by `make guardrails-server`
# from .env values — may contain API_KEY, never commit.
guardrails/safety/config.yaml
